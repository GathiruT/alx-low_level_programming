#include "main.h"
/**
 * mul - Multiplies two integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	int product = 0;

	product = (a * b);
	return (product);
}
